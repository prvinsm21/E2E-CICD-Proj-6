pipeline {
    agent any
    environment {
        DOCKERUSERNAME = "privnsm21"
    }

    stages {
        stage ('Git Checkout') {
            steps {
                sh 'echo Passed'
            }
        }
    }
}